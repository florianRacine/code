/**
 *    author:  evoPrg
 *    created: 27.08.2022 11:46:45
**/
#include <bits/stdc++.h>

using namespace std;

int findPath(int n, int k, int w, vector<int, int> path) {
    for (int i = 0; i < k; i++) {
        if (path[1] < n) {
            if (path[i][0] < i) {
                path[
            }
        }
    }
}

void outputVec(vector<int> vec) {
    for (int i = 0; i < (int) vec.size(); i++) {
        cout << vec[i] << " "
    }
    cout << endl;
}

int main() {
    /*
     *n weight of paths to found
     *k number of children for each node
     *d minimal weight of the path
     *w current weight
     */
    int n, k, d;
    cin >> n >> k >> d;

    return 0;
}
