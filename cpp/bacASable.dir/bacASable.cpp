#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
 
int32_t main() {
        
    int number;
    int sum = 0;

    for (int i = 1, i <= 5, i++) {
        cin >> number;
        sum += number;
    }
    std::cout << sum << std::endl;
    return 0;
}
